const URLAPI = "https://servicodados.ibge.gov.br/api/v1/localidades/distritos";
let filter = "";

let api_codigo_cidade = {
    get: function (callback) {
        fetch
        console.log();
    },

    post: function () { },

    set: function () { }
}